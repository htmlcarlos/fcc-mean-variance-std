# fcc-mean-variance-std
This program takes a list of nine numbers, transforms it into a 3x3 NumPy array, and calculates the mean, variance, standard deviation, max, min, and sum along both axes and for the flattened matrix. It includes input validation to ensure the list contains exactly nine elements, raising a ValueError otherwise.
